<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= guild.name %> - Kiyo Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="container py-4">
    <div class="row mb-4">
      <div class="col-md-12">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
            <li class="breadcrumb-item active" aria-current="page"><%= guild.name %></li>
          </ol>
        </nav>
        
        <div class="d-flex align-items-center mb-4">
          <% if (guild.iconURL()) { %>
            <img src="<%= guild.iconURL() %>" alt="<%= guild.name %>" class="me-3 rounded" width="80" height="80">
          <% } else { %>
            <div class="me-3 bg-secondary text-white rounded d-flex align-items-center justify-content-center" style="width: 80px; height: 80px; font-size: 2rem;">
              <%= guild.name.charAt(0) %>
            </div>
          <% } %>
          <h1 class="mb-0"><%= guild.name %></h1>
        </div>
      </div>
    </div>
    
    <div class="row">
      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-header">
            <h5 class="card-title mb-0">Server Stats</h5>
          </div>
          <div class="card-body">
            <div class="d-flex justify-content-between mb-2">
              <span>Total Members:</span>
              <span class="fw-bold"><%= stats.totalMembers %></span>
            </div>
            <div class="d-flex justify-content-between mb-2">
              <span>Humans:</span>
              <span class="fw-bold"><%= stats.humanMembers %></span>
            </div>
            <div class="d-flex justify-content-between mb-2">
              <span>Bots:</span>
              <span class="fw-bold"><%= stats.botMembers %></span>
            </div>
            <div class="d-flex justify-content-between mb-2">
              <span>Text Channels:</span>
              <span class="fw-bold"><%= stats.textChannels %></span>
            </div>
            <div class="d-flex justify-content-between mb-2">
              <span>Voice Channels:</span>
              <span class="fw-bold"><%= stats.voiceChannels %></span>
            </div>
            <div class="d-flex justify-content-between">
              <span>Roles:</span>
              <span class="fw-bold"><%= stats.roles %></span>
            </div>
          </div>
        </div>
        
        <div class="card">
          <div class="card-header">
            <h5 class="card-title mb-0">Quick Actions</h5>
          </div>
          <div class="card-body">
            <div class="d-grid gap-2">
              <a href="/dashboard/servers/<%= guild.id %>/commands" class="btn btn-outline-primary">Manage Commands</a>
              <a href="/dashboard/servers/<%= guild.id %>/settings" class="btn btn-outline-primary">Bot Settings</a>
              <a href="https://discord.com/channels/<%= guild.id %>" target="_blank" class="btn btn-outline-secondary">Open in Discord</a>
            </div>
          </div>
        </div>
      </div>
      
      <div class="col-md-8">
        <div class="card">
          <div class="card-header">
            <ul class="nav nav-tabs card-header-tabs">
              <li class="nav-item">
                <a class="nav-link active" href="#">Overview</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/dashboard/servers/<%= guild.id %>/commands">Commands</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/dashboard/servers/<%= guild.id %>/settings">Settings</a>
              </li>
            </ul>
          </div>
          <div class="card-body">
            <h5 class="card-title">Server Overview</h5>
            <p>Configure bot settings and view server statistics for <%= guild.name %>.</p>
            
            <div class="alert alert-info">
              <h5>Getting Started</h5>
              <p>To get the most out of Kiyo in your server, try the following:</p>
              <ul>
                <li>Configure command permissions in the Commands tab</li>
                <li>Set up auto-moderation settings in the Settings tab</li>
                <li>Set a welcome message for new members</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>